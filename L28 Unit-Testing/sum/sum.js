/**
 * While developing software we often write unit tests
 * to test our code.
 * Unit Testing - A way of testing individual pieces of
 * code called 'units'.
 * We use Jest(framework) to do unit testing.
 */

function sum(a, b) {
  return a + b;
}

module.exports = sum;
